div.container
    h2 Create tour own new message:
    h3 {{username}}
    p User name
    input(type="text", name="username", ng-model="username")
    p Message
    textarea(type="text", name="message", ng-model="message")
    div
        button.btn.btn-primary(ng-click="sendMsg(username, message)") Send